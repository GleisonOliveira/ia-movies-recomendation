# Project Agents

The skills folder is `.codex/skills`
All files are preprocessed using read_clean_file skill.
Does not create code comments
When working on the API project (in the `api` directory), refer to `api/AGENTS.md` for guidelines.
When working on the Frontend project (in the `frontend` directory), refer to `frontend/AGENTS.md` for guidelines.

